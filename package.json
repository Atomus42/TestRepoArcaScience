{
  "name": "arcascience-webflow",
  "version": "1.0.0",
  "description": "ArcaScience website — Webflow-ready assets, components, and custom code",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "dev": "npx serve dist"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
